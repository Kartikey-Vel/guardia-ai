# Core dependencies - these should install easily
numpy>=1.24.0,<2.0.0
opencv-python>=4.8.0
pillow>=8.3.0
setuptools>=65.0.0
wheel>=0.38.0
python-dotenv>=0.20.0 # For loading .env files
pymongo>=4.0.0 # For MongoDB
google-cloud-videointelligence>=2.0.0 # Ensure this is present
google-cloud-storage>=2.0.0 # For uploading videos to GCS

# Optional: lighter dependencies
scipy>=1.9.0
matplotlib>=3.5.0
click>=7.0

# Problematic packages (will be handled by Docker or manual install)

# Build dependencies (for manual installation)
cmake>=3.18.0

# Advanced Surveillance System Dependencies
face_recognition>=1.3.0
dlib>=19.22.0

# Performance monitoring
psutil>=5.8.0

# Additional AI/ML libraries
scikit-learn>=1.0.0

# Notification system dependencies
plyer>=2.1.0  # For cross-platform notifications
requests>=2.28.0  # For web notifications and API calls

# Enhanced features
geocoder>=1.38.0  # For location services
schedule>=1.1.0  # For scheduled tasks
python-telegram-bot>=20.0  # For Telegram notifications (optional)
twilio>=8.0.0  # For SMS notifications (optional)
